# trying to build a translator
def translate(phrase):
	translate = " "
	for letter in phrase:
		if letter in "AEIOUaeiou"
			translate = translate + "g"
		else:
			translate = translate + letter
	return translate

print(translate(print('enter a phrase')))